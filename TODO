-*- Text -*-

- Check for DVD I/O errors when starting and retry if necessary.

- Apparently no discont is being sent at the beginning.

- Find a way to automatically detect if fairpth is present and use it.

- BUG: Trying to terminate the program while in a still frame, blocks.

- BUG: The separate xvimagesink window flashes shortly before the
  program starts.

- Some menu subpictures get displayed too early (Matrix PAL). Why?

- Implement free changing of streams.

- Support angles.

- Implement support for other sound types.

- Implement the cell time as still time thing. Monsters Inc seems to
  use that in the warnings before the film.

- Put copyright/license notices in files.

- Add __richcmp__ to dvdread wrappers as necessary.

- Put pipeline handling in a separate class, wrapping the source
  element.

- Implement backwards playing.

- Implement proper VOBU jumping, using the jump fields in nav packets.

- Handle button nav packets properly with respect to time.

- Put source code in sf.net.

- Create a Pyrex wrapper for lirc.

- Honor prohibited operations when it makes sense, or ignore them
  smartly.

- Implement auto NTSC/PAL mode.

- LIRC component fails miserably when no LIRC kernel modules are
  available.


Particular DVDs
---------------

- Monsters Inc: Language selection still at the beginning doesn't show
  highlight.

- Frankestein: Doesn't go past menu.

- Jacky Brown: Funny menu highlight.

- The Others: Main menu doesn't show the highlight.
